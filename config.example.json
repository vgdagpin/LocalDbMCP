{
  "_note": "allowedTablePatterns is the only required field. Everything else can be supplied via mcp-server.ps1 CLI params (-ConnectionString, -AllowedTablePatterns, -ExcludedTablePatterns) — making this file optional when all params are passed via CLI.",

  "allowedTablePatterns": ["pr_*", "inv_*"],

  "__optional_fields": {
    "excludedTablePatterns": ["pr_sensitive_*"],
    "requestTimeout": 30000,
    "odbcDriver": "ODBC Driver 17 for SQL Server",
    "connectionString": "Data Source=(localdb)\\mssqllocaldb;Initial Catalog=YourDatabaseName;Integrated Security=SSPI;"
  }
}
